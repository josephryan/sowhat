
language: perl

dist: trusty
sudo: false

addons:
  apt:
    sources:
      - r-packages-trusty
    packages:
      - cpanminus
      - r-base-dev  
      - r-recommended
      - pandoc

cache:
  directories:
    - /usr/lib/R/library
      
before_install:
  - echo 'R_LIBS=/usr/lib/R/library' > .Renviron
  - echo 'options(repos = "http://cran.rstudio.com")' > .Rprofile
  - echo '.travis.yml' > .Rbuildignore
  - Rscript -e '{ install.packages("ape", dependencies = TRUE, lib = "/usr/lib/R/library")}'
  - cpanm -nf Statistics::R || cat /home/travis/.cpanm/work/*/build.log
  - cpanm -n JSON
  - ./build_3rd_party.sh ~