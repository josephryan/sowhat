language: perl

dist: trusty
sudo: false

before_install:
  - apt-get -qq update
  -  apt-get install -y r-base cpanminus unzip # gcc git
  - cpanm -n Statistics::R
  - cpanm -n JSON
  - Rscript -e "install.packages('ape', dependencies = T, repos='http://cran.rstudio.com/')"
  - ./build_3rd_party.sh
